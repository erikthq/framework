{
  "name": "example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "node --watch-path=. framework.config.ts",
    "start": "node framework.config.ts",
    "typecheck": "pnpm dlx --package=typescript tsc && pnpm dlx --package=typescript tsc -p src/scripts"
  },
  "dependencies": {
    "@erikt/framework": "workspace:*",
    "@erikt/framework-node": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.4.0"
  }
}
