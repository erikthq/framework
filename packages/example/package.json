{
  "name": "example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "node --watch-path=./ src/main.ts",
    "start": "node src/main.ts",
    "typecheck": "pnpm dlx --package=typescript tsc && pnpm dlx --package=typescript tsc -p tsconfig.scripts.json"
  },
  "dependencies": {
    "framework": "workspace:*",
    "node-adapter": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.4.0"
  }
}
